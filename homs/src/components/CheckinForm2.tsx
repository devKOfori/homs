import React, {useState} from 'react'

interface CheckinForm2Props {
  setDialogOpened: (opened: boolean) => void;
}

const CheckinForm2 = ({setDialogOpened}: CheckinForm2Props) => {
  return (
    <div>
      
    </div>
  )
}

export default CheckinForm2
